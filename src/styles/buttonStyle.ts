import { StyleSheet } from 'react-native';

export const buttonStyle = StyleSheet.create({
  btn: {
    backgroundColor: '#0047AB',
    borderRadius: 10,
    padding: 8,
    margin: 4,
    fontWeight: 'bold',
    textAlign: 'center',
    fontSize: 14,
    color: 'white',
  },
});
